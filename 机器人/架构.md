# 机器人版架构（复用 NP C 大脑 · sim2real）

> 更新：2026-08-28。接续 可行性调研.md。本文件是机器人版顶层架构：复用接缝、robot/ 目录、RobotExecutor、运动原语白名单与安全闸门、感知/语音、sim2real 验证。最小闭环已实现（brain.py，见 §9）。

## 1. 复用边界一句话
**三角色决策 + 落账口 + 记忆 = 原样复用（大脑）；"动作出口"抽成 ActionBackend，机器人侧换成 RobotExecutor（执行器）；A 审查从"游戏白名单"升级为"真机安全闸门"。**

| 层 | 位置 | 复用/新建 |
|---|---|---|
| B1 对话 · B2 编译 · A 审查 | npc/reviewer.py | 复用 |
| 落账口 book() · pending_task 语义 | npc/npc.py | 复用 |
| 记忆三阶段（反思/关联/遗忘） | npc/memory.py + npc/npc.py | 复用 |
| 动作出口（原 apply_action(world,...)） | 抽成 ActionBackend | 改接缝（默认=游戏） |
| 执行器 | robot/executor.py | 新建 |
| 真机安全闸门 | robot/safety.py | 新建 |
| 语音入（ASR）+ 出（TTS） | ASR 新增；TTS 复用 npc/tts.py | 各半 |
| Web 服务形态 | npc/server.py | 复用模式：只换注入 |

## 2. 顶层目录（现位于 D:\NPCSidekick\robot，独立于 1_Dagent）
```
robot/                    # 包名 = 目录名；tests/conftest.py 兜底导入路径
├── brain.py             # 机器人大脑：自主循环（编译/落账/tick/记忆+反思，Dagent 移植）
├── executor.py          # RobotExecutor(协议) + 工厂（sim/mujoco/real 三层）
├── safety.py            # SafetyGate：白名单/禁触/急停/电量/边界
├── primitives.py        # ALLOWED_MOTION_ACTIONS + 禁触字段（防篡改）
├── perception.py        # 机器人状态 -> 观察文本（喂给大脑上下文）
├── backend.py           # RobotBackend(ActionBackend) 桥接大脑->执行器（过闸不可绕过）
├── server.py            # HTTP 服务：/api/command /api/state /api/memory /api/estop + tick 循环
├── openclaw_skill/      # OpenClaw Skill 封装（SKILL.md，复制进 OpenClaw 的 skills 目录即用）
├── 启动机器人服务器.bat  # 双击启动（127.0.0.1:8766，sim 仿真）
├── store/               # 记忆卡落盘（robot_memory.json，服务启动时自动建）
├── tests/               # test_brain / test_server / test_robot_safety / test_executor_stub / test_sim_smoke
└── sim/                 # 仿真层
    ├── stub.py          # SimEntity 最小实现（无 3D，先跑链路）
    ├── mujoco.py        # MujocoEntity：unitree_mujoco 无头物理步进
    ├── dds_sim.py       # 无头 DDS 仿真：官方桥 + 物理步进（rt/lowcmd 受控，--selftest 自检）
    ├── dds_entity.py    # DdsEntity：DDS 电机级执行器 v1（trot 直线巡逻，参数为实测所得）
    ├── run_sim.py       # 仿真冒烟入口
    ├── unitree_mujoco/  # 宇树官方开源（上游原样保留）
    └── unitree_sdk2_python/  # 宇树官方开源 SDK（真机桥 M3 用，上游原样保留）
```

## 3. 复用接缝：ActionBackend
大脑只管"决定做什么"（move/gather/deliver/...），不关心动作落在游戏世界还是真机身上。原代码在 npc/npc.py::run_task 与 scheduler 里直接调 npc.world.apply_action(world, ...)。最小改动是给 NPC 注入一个动作后端：
```
class ActionBackend(Protocol):
    def observe(self) -> dict: ...
    def apply(self, action: str, params: dict) -> tuple[bool, str]: ...
class GameBackend(ActionBackend):
    """游戏后端 = 现状：直接改世界字典 apply_action。"""
```
- 默认 GameBackend，游戏 NPC 行为零回归（用同一套测试守住）。
- 机器人 RobotBackend 把 move/gather/deliver 转发给 RobotExecutor。
- 落账口语义不变：book() 仍是唯一生 pending_task 的入口；承诺成立 = 被推进活动队列，只是"推进"到执行器而非世界字典。

## 4. RobotExecutor
```
class RobotExecutor(Protocol):
    def move_to(self, x, y, yaw) -> bool: ...
    def navigate(self, waypoints) -> bool: ...   # 供"走到房间/巡检"
    def grab(self, target: str) -> bool: ...
    def release(self) -> bool: ...
    def pose(self) -> dict: ...                  # x,y,yaw,电量,imu,急停态
    def estop(self) -> None: ...
class SimEntity(RobotExecutor):
    """仿真执行器：unitree_mujoco（真实物理仿真）/ stub（无 3D 先跑链路）。"""
class UnitreeReal(RobotExecutor):
    """真机执行器：unitree_sdk2 / unitree_ros2 下发（M3 阶段引入，接口一致）。"""
```
三层一致：stub（纯逻辑/单测）-> SimEntity（unitree_mujoco，sim2real 校验）-> UnitreeReal（真机）。切换只改一处注入。

## 5. 运动原语白名单 + 安全闸门（防篡改 / 真机安全）
```python
# primitives.py —— 对应 npc/reviewer.py::ALLOWED_TASK_ACTIONS 的机器人版
ALLOWED_MOTION_ACTIONS = ("move_to", "navigate", "grab", "release")
# 重申 npc 铁律：LLM 零关节/零文件直控；只允许白名单原语
FORBIDDEN_MOTION_TOKENS = {"file", "path", "write", "read", "exec", "shell", "open"}

# safety.py —— A 审查升级为真机安全闸门（参照 Claudia 三级电量 + 非绕过门）
class SafetyGate:
    def check(self, action, params, robot_state) -> tuple:
        # 1) 动作必须命中 ALLOWED_MOTION_ACTIONS
        # 2) 禁触字段/值 -> 拒
        # 3) 急停标志已置位 -> 全拒
        # 4) 电量门控：<=10% 仅安全原语 / <=20% 拒高能耗 / 正常全量
        # 5) 目标坐标必须在可达边界内 + 速度/力矩上限
```
SafetyGate.check 是不可绕过的（所有 RobotExecutor 调用先过它，同 npc 的"落账口不可绕过"思想）。B1 生成文案、B2 编译任务、A 审查 + 安全闸双重放行后才下发。

## 6. 感知与语音
- 感知入：perception.py 把 pose()/imu/电量/避障告警/需抓取目标名 拼成"观察文本"，喂进 NPC 上下文（对应 npc/world.observe），复用"记忆只讲真实发生事，不编造"的接地约束。
- 语音出：复用 npc/tts.py，/api/command 返回 {reply, audio}（同游戏端模式）。
- 语音入（新增 ASR）：可选 faster-whisper（本地，离线），/api/command 接 {audio} 或 {text} 两种入参。

## 7. 一次"指令 -> 执行"时序
```
用户文本/语音
  -> B2 compile_task()            （复用：机器人意图词表，扩展"巡检/拿/放/去房间"）
  -> A review_task()              （复用：可行性 + 白名单 + 禁触字段）
  -> SafetyGate.check()           （新建：真机安全，附加一关）
  -> 落账 book()->pending_task     （复用：唯一入口）
  -> RobotBackend.apply(步骤)      （通过 RobotExecutor -> stub/sim/real）
  -> 结果/新状态 -> perception -> 记忆回流（maybe_reflect/consolidate 复用）
```

## 8. 测试与验证路径
复用 Dagent 测试风格（pytest -q -p no:cacheprovider，不写 .pytest_cache）：
| 层 | 测试 | 对应现役参考 |
|---|---|---|
| 白名单/安全闸 | test_robot_safety.py | test_reviewer.py / test_security.py |
| 编译/落账 | test_robot_brain.py | test_npc.py |
| 执行器 stub | test_executor_stub.py | test_npc_tools.py |
| sim 冒烟 | test_sim_smoke.py（unitree_mujoco 步进不炸） | - |
| DDS 闭环 | test_dds_sim.py（子进程跑 dds_sim --selftest） | - |
| 指令→走 | test_dds_walk.py（一句话→物理仿真走 2m，子进程） | - |

验证顺序：单测 stub -> sim 链路冒烟 ->（有真机后）sim2real 对照真机。

## 9. 当前状态 / 待办
- [x] 可行性调研（可行性调研.md）
- [x] 顶层架构（本文件）
- [x] robot/ 包骨架：primitives.py + safety.py + executor.py(stub)
- [x] 最小闭环：文本指令 → B2 编译 → 预审+落账 → stub/仿真执行 → 记忆+反思回流
      （brain.py::RobotBrain，2026-08-28，自主日常=巡逻/待命/低电量回桩，test_brain 17 passed）
- [x] unitree_mujoco 物理仿真接入（robot/sim/mujoco.py::MujocoEntity + build_executor('mujoco')）
- [x] server.py：/api/command 复用 npc.server 模式（POST 接单 / GET 状态与记忆 / POST 急停；
      急停中自主循环停摆不刷失败；lifespan 每 3s 推一帧；中文 GBK 请求体兜底，test_server 6 passed）
- [x] OpenClaw Skill 封装（openclaw_skill/SKILL.md，2026-08-29）：照 go2-openclaw-skill 格式，
      核心铁律 = 狗的 reply 原话转述不许编造（落账口语义对外输出）；四类动词对照表 +
      故障处理；全部 curl 指令对真服务逐条实测通过
- [x] DDS 电机级控制（2026-08-28）：cyclonedds 11.0.1 + unitree_sdk2py（pip install -e
      --no-deps 跳过钉死的 0.10.2，API 兼容）在 Py3.13/Windows 直装可用，"需源码编译"已不成立。
      sim/dds_sim.py = 无头 DDS 仿真（官方桥；timerfd 用纯线程垫片替换，上游零改动），
      rt/lowcmd 站立序列 → 物理起立 → rt/lowstate 校验（--selftest；test_dds_sim 以子进程
      跑，因 cyclonedds 原生库退出时段错误，不污染 pytest 退出码）
- [x] 指令 → 走 v1（2026-08-29，直线巡逻员）：trot 步态参数实测调出（freq 2.0Hz /
      摆幅 0.25/0.8 / Kp80Kd4，~0.7m/s 稳定不摔），DdsEntity 分段自适应走到即停。
      全链路已焊死：一句话"去桌边" → 落账 → DDS → 物理仿真真走 2m → 到点记账
      （test_dds_walk）。v1 限制（诚实拒绝并记"没做成"）：不会转弯（左右差动会摔、
      差频只单向有效，待调）、没装手臂(grab/release=False)、y 向漂移 ~10% 不闭环。
      下一版：弧线/原地转弯调参、位姿航向闭环、速度自整定
- [ ] 真机 SDK 桥（M3，需授权）：RobotExecutor → unitree_sdk2py 的 Go2 SportClient
      （Move/StandUp/StopMove 等小脑动词，sim/unitree_sdk2_python/example/go2/high_level 有示例；
      DDS 管道已验证可用，差的是真机授权与实物验证）

## 10. 跑测试

在本目录（D:\NPCSidekick）下执行：
```
python -m pytest robot/tests -q -p no:cacheprovider
```
tests/conftest.py 会把上级目录插进 sys.path 兜底导入（曾用名 4_robot 时的注册分支也保留，改回去照样能跑）。

启动服务（可选）：双击 `robot\启动机器人服务器.bat`，或
```
python -m robot.server          # 127.0.0.1:8766；--executor mujoco 换物理仿真
```
发话：POST /api/command {"text": "去门口"}；看状态 GET /api/state；急停 POST /api/estop。

接进 OpenClaw（可选）：把 `robot/openclaw_skill/SKILL.md` 复制到 OpenClaw 的 skills 目录
（如 ~/.openclaw/skills/npc-sidekick-dog/），之后对代理说"让狗去门口"即可——
它会把话转给 /api/command，并把狗的原话回复带回来。