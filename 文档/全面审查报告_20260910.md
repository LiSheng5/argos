# ArgOS 全面审查报告

> 审查日期：2026-09-10 · 审查方式：只读（未修改任何代码）· 独立取证
> 被审对象：D:\ArgOS（宇树 Go2 机器狗 AI 大脑）— 自研大脑约 3.5k 行 Python + React Console
> 口径：凡数字与状态均**实测**，不引用文档自述（文档正是被审对象之一）
>
> **修复状态（2026-09-10 当日）**：P1-1、P1-2、P2-1~P2-7、P3-1~P3-5 已全部修复；
> 回归测试 `149 passed, 3 skipped`（含新增 Origin 回归锚），前端 `vite build` 通过。

---

## 0. 一句话结论

**核心质量高、安全设计扎实，未发现 P0 级缺陷。** 主要问题集中在**文档与实况的口径漂移**（尤其测试数字）与 **Web 层的几处纵深防御加固**，均不触及大脑/安全闸的功能正确性。

| 维度 | 结论 | 分级计数 |
|---|---|---|
| 测试基线 | ✅ 稳定，但文档口径不符（见 P1-1） | **P0 × 0** |
| 核心安全（SafetyGate / 急停 / 看门狗） | ✅ 设计完整、fail-closed 到位 | **P1 × 2** |
| 架构铁律（LLM 只提议 / to_thread / 缺数据=null） | ✅ 逐条落地 | **P2 × 7** |
| 记忆与反思闭环 | ✅ 已真正接上（recall 有调用方） | **P3 × 5** |
| Web Console | ⚠️ 合规，但锁与 Origin 可加固 | |
| 文档一致性 / 工程卫生 | ⚠️ 1 处硬伤 + 若干小不一致 | |

---

## 1. P1（应尽快处理）

### P1-1 文档"151 passed, 0 skipped"与实测不符
- **实测**：`python -m pytest tests -q -rs` → **`148 passed, 3 skipped, 1 warning`**（8.6s）
- **skip 原因**（实测）：`test_dds_sim / test_dds_walk / test_dds_closed_loop` 均 `pytest.importorskip("unitree_sdk2py")` 失败 → `No module named 'unitree_sdk2py'`（上游 SDK 未安装；本仓库按设计用 .gitignore 排除上游，需按 requirements.txt 第 4 节重拉）
- **矛盾点**：
  - `README.md:9` "**151 项测试全绿**"、`README.md:94` "`# 151 passed, 0 skipped`"
  - `文档/小结_20260829.md:6` "**151 个测试全绿，0 个 skip**"
  - `文档/项目书_宇树天才少年.md:51,82` 同口径（该文件 gitignore 不入库，本地保留）
- **建议**：改为诚实双口径 —— "**148 passed + 3 skipped**（DDS 三例需先按 requirements 拉上游 SDK，未装自动跳过）；装了上游后为 151 passed"。这正好符合本项目自己的规矩："数字要标截至日、状态一律实测"。

### P1-2 `文档/架构.md` §2 目录树漏列核心文件
- **实测**：`argos/` 实际含 `memory.py`、`llm.py`、`persona.py`、`persona.json`、`__init__.py`，但 §2 目录树（第 22–43 行）**一个都没列**（仅在 §9 正文顺带提过）
- **影响**：架构.md 是"单一信息源"，目录树与实况不符会误导后续开发（尤其 memory/llm 是大脑核心）
- **建议**：把这几个文件补进 §2 目录树

---

## 2. P2（择机修）

| # | 位置 | 问题（附证据） | 建议 |
|---|---|---|---|
| P2-1 | `server.py:44`、`web/api.py:371` | Origin 校验用**前缀匹配** `origin.startswith("http://127.0.0.1")` → `http://127.0.0.1.evil.com` 也会通过 | 改精确比对（`origin in {...}`）或加 `$` 锚点；两处口径统一 |
| P2-2 | `web/bridge.py:92` `Console` | 中枢**无锁**：`active_run` 被 `submit()`（`api.py:134` 经 to_thread）与 `tick_sink()`（tick 线程）并发读写（`bridge.py:128/242/252/257`）→ 可能丢失/覆盖 Run 状态 | 给 `Console` 的共享字段加 `threading.RLock`，或收敛为单入口 |
| P2-3 | `web/api.py:55-73`、`ws.py:105` | 只读端点 `api_brain / api_robot_state / api_robot_telemetry` 及 WS 遥测**在事件循环里同步调** `brain.status()/observe()`，与 tick 线程并发访问同一状态且**未加锁**（仅 `robot_state` 包了 try）→ 偶发读到半更新态 | 这些端点也走 `to_thread`，或在 brain 读取口加读锁 |
| P2-4 | `文档/架构.md:3` | 死链接：写"接续 **可行性调研.md**"，实际文件名是 `ai搜索后相关项目后做的可行性调研.md` | 统一引用真实文件名 |
| P2-5 | `文档/小结_20260829.md:8` | 悬空引用："再按文末的'**下次开工指南**'接上"，但全文只到 §7，无此章节 | 补章节或删引用 |
| P2-6 | `requirements.txt` | `argos/web/camera.py:159` `import cv2`（本机 USB 相机路径）**未声明依赖**、也未标"可选"（对比 chromadb/jieba 已注明） | 加 `opencv-python  # 可选，USB 相机` |
| P2-7 | `.gitignore` | 缺 `.pytest_cache/`：实测 `git check-ignore .pytest_cache` → **NOT IGNORED**（当前未被 tracked，但根目录已存在该目录，易被误提交） | 补 `.pytest_cache/` |

---

## 3. P3（低危 / 加固）

| # | 位置 | 问题 | 建议 |
|---|---|---|---|
| P3-1 | `argos/server.py:3` | docstring 写 `[--executor sim\|mujoco]`，实际 `executor.py:19` 支持 `sim/mujoco/dds/real` 四值 | 补全 dds/real |
| P3-2 | `文档/架构.md` §2 `console/` 段 | 目录树漏 `App.jsx / api.js / main.jsx / components/ / styles.css` | 补全或注明"省略非核心" |
| P3-3 | `web/api.py:112,346,359` | `/api/robot/image/{name}`、`/api/camera/stream.mjpg`、`/api/camera/snapshot.jpg` 缺 `Depends(_verify_origin)` | 补 Origin 校验（本地工具，危害有限） |
| P3-4 | `web/camera.py:75` | 外部 URL 转发流用 `httpx.AsyncClient(timeout=None)`：上游卡住则响应长期挂起（MJPEG 长连接本身合理，属加固） | 加读超时 / 空闲超时 |
| P3-5 | `console/src/pages/Robot.jsx`、`store.jsx` | 前端：profile 编辑副作用会被 `refresh()` 重置（输入丢失）；卸载未 `clearTimeout(timerRef)`（StrictMode 轻微泄漏） | 首次挂载初始化 + cleanup 清理 |

---

## 4. 值得肯定的地方（不是问题，是资产）

- **安全闸双道不可绕过**：`book()` 预审 + `backend.apply()` 执行前再 check；急停 `RobotBackend.estop()` **同时**置闸门与执行器（P0-1 已根治）；电量未知 **fail-closed**（`safety.py:75`）。
- **真机安全设计到位**：`watchdog.py` 跳闸后**不自动恢复**（断一次就停下等人）；`real_sport.py:213` `move_to` 用 `try/finally` 保证**任何异常先收速**；`grab/release` 无机械臂时**诚实返回 False 不假装**。
- **反思闭环已真正接上**：`brain._recall_context()`（话术层）+ `_instruction_hints()`（行为层）**给 `recall()` 找到了真实调用方**，此前"反思只写不读"的问题已解决；并带三层防编造（evidence 溯源 / 世界实体 / 重要性夹取）。
- **Web 不侵入原则兑现**：`InstrumentedBackend.apply()` **返回值原样透传**（`bridge.py:89`）；`tick_sink=None` 时与旧版逐字等价；缺数据一律 `null`，无造假。
- **测试分布合理**：148 例覆盖编译/落账/安全/记忆/反思/位姿/看门狗/Web，`test_estop_and_tick` 专门钉"急停到达执行器 + tick 不阻塞事件循环"。

---

## 5. 建议行动顺序

1. **改文档测试口径**（P1-1）—— 对外声明的诚实性，改动最小、收益最大。
2. **补架构.md §2 目录树 + 修死链/悬空引用**（P1-2、P2-4、P2-5、P3-2）—— 一次性把文档对齐，顺手做。
3. **补 `.pytest_cache/` + `opencv-python` 声明**（P2-6、P2-7）—— 一行的事。
4. **Web 层加固**（P2-1 Origin 精确匹配、P2-2 Console 加锁、P2-3 只读端点 to_thread、P3-3 Origin、P3-4 超时）—— 可在下一轮 Console 迭代里一起做。
5. **前端小修**（P3-5）—— 体验问题，随缘。

> 观察项（非缺陷）：工作区当前有 3 个**未提交**的文档改动（`文档/Console_使用说明.md`、`文档/小结_20260829.md`、`文档/架构.md`），与记忆记录一致（"先不 commit，用户自己过一遍"）。

---

*本报告所有结论均可复现：测试用 `python -m pytest tests -q -rs`；git 用 `git status --short` / `git check-ignore -v`；文档引用用 `grep -n`。*
