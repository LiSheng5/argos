# 2026-08-29 补（评审 P0-3）
__pycache__/
*.py[cod]
*.egg-info/

# 运行时产物
store/                  # 记忆卡 robot_memory.json，服务启动时自动建
*.bad-*.json            # 坏记忆卡改名留证（brain._quarantine_memory）

# 宇树官方上游：自带 .git，体积大，不进本仓库。
# 按 requirements.txt 第 4 节的命令重新拉取。
argos/sim/unitree_mujoco/
argos/sim/unitree_sdk2_python/
