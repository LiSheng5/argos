# 2026-08-29 补（评审 P0-3）
__pycache__/
*.py[cod]
*.egg-info/

# 运行时产物：记忆卡 robot_memory.json（store/），坏卡改名留证（*.bad-*.json）
store/
*.bad-*.json

# 密钥（未来接 LLM 用，铁律：绝不入 git）
api_key*.txt
.env

# 宇树官方上游：自带 .git，体积大，不进本仓库。
# 按 requirements.txt 第 4 节的命令重新拉取。
argos/sim/unitree_mujoco/
argos/sim/unitree_sdk2_python/

# 项目书（申请文书，不公开，本地保留）
文档/项目书*.md
